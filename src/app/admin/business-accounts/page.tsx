import { BusinessAccountsManagement } from '@mod-admin/presentation/components';

/**
 * Business accounts management page.
 * Admin interface for managing business accounts (SUPER_ADMIN only).
 */
const AdminBusinessAccountsPage = () => {
  return <BusinessAccountsManagement />;
};

export default AdminBusinessAccountsPage;
