import { VenuesManagement } from '@mod-admin/presentation/components';

/**
 * Admin venues management page.
 * Provides full CRUD functionality for venues.
 */
const VenuesPage = () => {
  return <VenuesManagement />;
};

export default VenuesPage;
